<% if user_signed_in? %>
  <li>
  <%= link_to(('<i class="icon-off"></i> Logout').html_safe, destroy_user_session_path, :method => :delete) %>        
  </li>
<% else %>
  <li>
  <%= link_to(('<i class="icon-off"></i> Login').html_safe, new_user_session_path)  %>  
  </li>
<% end %>