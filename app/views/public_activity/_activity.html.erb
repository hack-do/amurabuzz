<% type_of_activity.each do |activity| %>
<%# activity.inspect %>
	<div class="container-fluid">
	  <div id="<%= "activity#{activity.id}"%>"class="activity row">
			<div class="col-md-7 col-md-offset-2">
	  		<% if activity.owner.id == @current_user.id %>
	  			You
	  		<% else %>
		    	<%= link_to activity.owner.user_name, user_path(activity.owner.id) if activity.owner %>
		    <% end %>
		    <%= render_activity activity %>
	    </div>
	    <div class="col-md-3">
		    <small><em>
		    	<%= distance_of_time_in_words_to_now(activity.created_at) %>
	    	</em></small>
		</div>
	  </div>
	</div>
<% end %>