$("#like_modal_body_table").text("");
<% @likers_a.each do |l|%>
	$("#like_modal_body_table").append("<tr><td><%= escape_javascript link_to l.user_name,my_friend_path(id: l.id) %></td></tr>");
<% end %>